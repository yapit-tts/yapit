# Named .env.production (not .env.prod) because Vite reads this filename by default for production builds

# Fill after creating Stack Auth project via dashboard
VITE_STACK_AUTH_PROJECT_ID=FILL_AFTER_DEPLOY
VITE_STACK_AUTH_CLIENT_KEY=FILL_AFTER_DEPLOY

# Relative URL - nginx proxies /api/* to gateway
VITE_API_BASE_URL=/api

# WebSocket URL - needs full URL for production
# TODO: useWS.ts is currently dead code; if needed, update to detect protocol dynamically
VITE_WS_BASE_URL=wss://yaptts.org/api
